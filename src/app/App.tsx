import { Calls } from "components/pages";

import classes from "./App.module.scss";

function App() {
  return (
    <main className={classes.app}>
      <Calls />
    </main>
  );
}

export default App;
